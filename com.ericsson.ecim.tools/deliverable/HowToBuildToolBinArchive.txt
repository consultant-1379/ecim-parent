
To manually create the bin archive, you need to install maven. The .m2/settings.xml must define the ericsson proxy for external access.

Then in the com.ericsson.ecim.tools project type
>mvn clean package assembly:single

The artifact is created in the target folder.